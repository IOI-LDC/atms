## Reconcile outdated Phase 1 documentation

**Scope confirmed:** Gap analysis + cross-doc fixes. G-01 (ERP-as-source-of-truth) stays an open decision but gets explicit cross-linking so docs stop silently contradicting.

All changes are doc-only — no code, no migrations, no tests.

### 1. `docs/PHASE_1_GAP_ANALYSIS.md` (primary)
- **Add Revision 3 header note (2026-07-11):** G-03 closed; dashboard KPIs + relocated-assets widget shipped; self-service password change shipped; email transport clarified.
- **G-03 → CLOSED:** rewrite §4.3 (remove the stale pre-fix code snippet quoting the `TODO`; replace with the shipped role-conditional logic at `useLocations.ts:28-34`); update the severity table (line 61), §3.9 table + verdict (lines 206-223), §12 action plan (line 573 — remove the "🔴 P0" row), and §13 risk R-03.
- **G-11 → CLOSED:** §9.7 (line 523) and §3.12 table (line 244) — the "Recently Relocated Assets" widget shipped via `GET /api/dashboard/kpis` (`RecentlyRelocatedAssetsQuery`, `DashboardKpiResource`, frontend `useDashboardKpis.ts`). Move to Appendix A "complete" list.
- **Newly-shipped features note:** add a short "Shipped after Revision 2" subsection — self-service password change (`POST /api/auth/change-password` + FE UI), Dashboard KPI tiles + relocated-assets widget. This corrects the snapshot without rewriting every section.
- **Email (I-03 / R-06 / I-05) → align to your updated position:** Graph sendMail is the only production transport; Power Automate retired (not a fallback); Phase 1 email scope = activation + password-reset only; MR/WO operational emails outside current Phase 1. Rewrite I-03 (line 561) and R-06 (line 617). Replace I-05 (SharePoint RuntimeException, line 563) — it references the retired PA transport and is superseded.
- **§12 "Next review" line (line 649-650):** drop "after G-03 is closed" (it now is) → next review is before Phase 3 / when G-01 is decided.

### 2. `docs/05-delivery/TDL.md`
- **G-03 row (line 163):** change 🔴 Critical → ✅ Done, update file ref, mark closed 2026-07-03 (commit `de85abe`).
- Leave the §10 G-01 entry as the canonical open decision (this is where README/RISKS/CLAUDE will point).

### 3. `.kilo/TLD.md`
- Add a "Recently Completed" row for **G-03 closed** (the location-picker fix shipped in `de85abe` but was never logged here — this is the parallel-track drift).
- Update the 🔵 External Blockers #4 email row: remove "Power Automate webhook URL" residue and align to Graph-remaining items (Application Access Policy + frontend subdomain) — matches your new email position.

### 4. `CLAUDE.md` Phase 1 section (lines ~495-505)
- Line 502: G-03 → CLOSED (it currently calls it "the one remaining Critical gap"). Update the §"Frontend has 2 stub views" line to stay accurate (G-05/G-06 stubs remain).

### 5. `docs/README.md` + `docs/05-delivery/RISKS_AND_ASSUMPTIONS.md` — G-01 cross-link
You chose "leave open, cross-link explicitly" for G-01. These two docs state "no ERP asset sync" as a locked assumption (README line 31, 34; RISKS line 13-14), contradicting TDL #10 / gap-analysis §4.1 / CLAUDE which treat it as open.
- Add a one-line **OPEN DECISION (G-01)** callout under the "Source-of-truth boundaries" section in README and under the relevant assumption bullet in RISKS, pointing to `docs/05-delivery/TDL.md` #10 as the single canonical place. Wording flags that the decision is pending and the "Add Asset" button is disabled pending the call. **Not resolving the decision** — just stopping the silent contradiction.

### 6. Phase-numbering collision note
The commercial Phase 1/2/3 (Two-Phase Proposal, SCOPE_CHANGE, TDL) vs build Phase 0–10 (IMPLEMENTATION_PLAN) schemes collide. SCOPE_CHANGE.md line 80-81 already notes this — I'll add a short pointer in `docs/05-delivery/IMPLEMENTATION_PLAN.md` pointing back to SCOPE_CHANGE.md so the collision is documented from both sides (currently only noted in one).

### Not in scope
- Product/design docs in `docs/atms/` (per your "gap analysis + cross-doc" choice).
- Resolving G-01 itself (business decision — left open with cross-links).
- The build-vs-commercial phase renumbering (just clarifying the collision exists).

### Verification
Doc-only changes — no tests to run. I'll grep after edits to confirm: no remaining "G-03" + "Critical" co-occurrence; no remaining "one remaining Critical gap"; Power Automate no longer framed as a fallback in the touched files.