1) Sheet Size is too small, make it larger, at least twice on Desktop. Mind Tablet and Mobile (I think it should be responsive and almost full screen).
2) Maintenance Requests page.
a) Add Filter to Request and Asset and Created.
b) Add pagination (to all tables)
c) VISUAL (Header background is dark, make it slightly lighter), second header has a gap with first header, fix please.
3) Edit Maintenance Request page. the form Layout is bad, make it better by proper alignment. It is too dense on the left side with lots of empty space on the right. Add more gutter between form rows.
Also it has no back button. It should have a back button to go back to the previous page (ensure to be on the right tab, with table pagination, sort anf filters preserved)
4) Sonner has no background colour based on status. all are white.