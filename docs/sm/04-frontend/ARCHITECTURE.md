# Store Management (SM) — Frontend Architecture

> **Status:** Placeholder. SM has no frontend yet.

SM will follow the same frontend stack as ATMS: **Vue 3 + TypeScript + Vite + Tailwind CSS + shadcn-vue + Pinia**. The SM frontend will live in a future `sm/` source directory and consume the shared Laravel API under `/api/`.

No components, routes, or stores exist yet.
