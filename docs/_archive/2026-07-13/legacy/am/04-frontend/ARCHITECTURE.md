# Asset Movement (AM) — Frontend Architecture

> **Status:** Placeholder. AM has no frontend yet.

AM will follow the same frontend stack as ATMS: **Vue 3 + TypeScript + Vite + Tailwind CSS + shadcn-vue + Pinia**. The AM frontend will live in a future `am/` source directory and consume the shared Laravel API under `/api/`.

No components, routes, or stores exist yet.
