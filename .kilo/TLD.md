# Task List — Development Tracker

> **Purpose:** Single source of truth for what is being built, what was completed
> that the other team needs to know about, and what was deferred so it doesn't
> get lost.
>
> **Update this during or immediately after every build session.** It is short
> by design — if a task takes more than 2 minutes to write down, it is too big
> and should be split.

---

## 🔴 In Progress

<!-- What someone is actively coding right now. Max 2 items per team. -->

| Team | Task | Started |
|---|---|---|
| Full-stack | 🔴 **Status vocabulary — design agreed 2026-08-16, NOT implemented.** Record corrected 2026-08-15 (earlier "AGREED then reopened" tracker claims were false). Final design in `docs/plans/2026-08-07-operational-status-vocabulary.md`: `operational_status` → 4 machine values (`ready_for_field`/`under_maintenance`/`failure` [renamed from `down` — LDC reads "down" as waiting-for-parts]/`at_the_field` auto-set on rig/well_site location change); WO close always `ready_for_field`; cancel keeps caller choice; `assets.condition_status` (UI "Condition", admin-editable group `asset_conditions`) = `need_assembly`/`missing_parts`/`need_inspection` + default `normal` (**confirmed 2026-08-16 and seeded in 4a** — no longer proposed) with reset-on-close (warn if Need Inspection + PM not marked); LIH/DBR/Scrapped have no label home (categorical reporting needs a distinguishing source — open); `assets.erp_status` removed; `maintenance_sub_status` deprecated; Phase 1.5 cancelled (inspection = PM). RQ1–RQ4 captured (plan §7). **LDC answered the blocking questions 2026-08-16** (Q1/Q2 `at_the_field` precedence + field-exit rules, Q3 no validity matrix, Q4 approval location = Tajoura Base/keep-current, Q5 PM marking both times, Q6 ERP quantity authority + WO decrement, Q8 identifier split) — **and Q7 was answered NO on 2026-08-16**, cancelling R-11/Phase 8. **No LDC question remains open on this programme.** R-10B dead — not restorable. **Execution companion:** `.kilo/plans/2026-08-16-status-vocabulary-implementation.md` — 8 phases, reviewed three times; Phase 1 (unified asset-eligibility guard, 8 surfaces) is the only prerequisite for Phase 4; Phase 4 is a 3-release expand/switch/contract with a documented downtime cutover. ✅ **Location prerequisite met (2026-08-17):** `RIG A` (rig), `Well A` (well_site) and `Tajoura Base` (yard) all exist on dev, so `at_the_field` and the Q1/Q2 location rules are live, not dormant. Confirm the same on prod with `./scripts/survey-data.sh` before deploying 4b. Implementation order: 1) ✅ doc hygiene **(done)**, 2) ✅ `is_active` MR/WO/PM gating fix — including direct/manual PM evaluation and the scheduler's `scopeEvaluable` **(done 2026-08-16, see 🟡 above)**, 3) ✅ RQ4 — `erp_part_code` everywhere **(done 2026-08-16)**, 4) ✅ parts decrement/restore **(done 2026-08-16)**, 5) ✅ vocabulary axis + condition field — **4a (additive) and 4b (the switch) both done 2026-08-16**; 4b's deploy needs a maintenance window, see `docs/RELEASE-4b-CUTOVER.md`. **4c remains**: drop **`erp_status` only** — an ordinary `deploy.sh` release once 4b has run cleanly for a few days. ⚠️ **`maintenance_sub_status` is NOT dropped**: its readers went in 4b, but the column is retained until Phase 2 Assembly is specified (🟠 P2-001 derives `installed`/`ready` from `parent_asset_id`, so it should not be needed — but 400 NULLs cost nothing and an early drop would have to be undone if that spec changes). **Phase 4 is now unblocked — Phase 1 was its only gate.** | 2026-08-16 |
| Full-stack | ✅ **User provisioning redesign — drop SharePoint directory, direct user creation with activation email.** **DONE 2026-08-04:** `POST /admin/users` + Create User dialog (name, email, role); activation email; @ldc.com.ly allowlist (`ATMS_ALLOWED_EMAIL_DOMAINS`); employee-directory infrastructure removed (EmployeeDirectorySource, employee endpoints/UI); tests updated. | 2026-08-04 |
| Full-stack | ✅ **Asset location at Tajoura Base TJB + operational status changes.** **DONE 2026-08-04:** location "Tajoura Base" (code TJB, type yard) created and all assets relocated via migration with movement history; OperationalStatus DB values renamed (active→ready_for_field, inactive→scraped) plus new under_inspection and lih — six statuses total; WO close/cancel choice down \| ready_for_field; migrations effective on VPS via deploy.sh. | 2026-08-04 |
| Full-stack | **Reports — CSV export part-done (D-010), UI complete for what exists.** Mechanism built (`CsvReportStreamer` + `ReportCsvColumns`, `?format=csv` on existing endpoints, UTF-8 BOM, Tripoli dates, true streaming) and wired to **3 of 21** reports: R-1, R-2, R-22 — each now has a working **Export CSV** button. **Remaining 18 are backend-only work:** 10 cursor reports need a `stream` closure beside their paginator + a column map; 8 aggregate reports need only a column map. Add the button to a report **only after** its endpoint honours `format=csv`, or it downloads JSON under a .csv name. Then R2/R3 once LDC defines them (🔵 #5). | 2026-08-01 |

---

## 🟡 Recently Completed — Frontend / Other Teams Need to Know

<!-- Backend finished something? Put it here so frontend knows the API changed.
     Frontend finished a screen? Put it here so others know the UX is done.
     Items stay here until the other team acknowledges. Then move to Done. -->

| Date | What changed | Who needs to know | Status |
|---|---|---|---|
| 2026-09-02 | ✅ **Production topology moved to single-host, same-origin — `assets.ldc.com.ly`.** No app code changed (already origin-agnostic by design); `Caddyfile`, `.env.production.example`, `frontend/.env.production`, and `deploy.sh` updated. New `docs/VPS-PROVISIONING.md` runbook for a fresh VPS. `SESSION_DOMAIN`/`SANCTUM_STATEFUL_DOMAINS` now the single host, `CORS_ALLOWED_ORIGINS` effectively moot for the SPA. **Not done: an actual VPS deploy** — needs a real `.env` with production secrets filled in on the target VPS, and DNS pointed at it. See `.kilo/STATE.md` 2026-09-02 for full detail. | Ops | ⚠️ Unacknowledged |
| 2026-08-30 | ✅ **RQ2 attachment gate REMOVED — a work order now closes without any attachment.** LDC direction: some jobs have no file to attach and must still be closable. **API change:** `POST /work-orders/{id}/close` **no longer returns 409** on an empty attachments list — the guard in `CloseWorkOrder` is gone and the close succeeds with zero attachments. Everything else about the upload window is unchanged: uploads stay open on a **completed** WO for the assigned technician and Admin/Manager, and stay encouraged; uploads **and** deletion still 403 once closed or cancelled; cancel still needs no attachment. Frontend close dialog no longer blocks the button or shows the no-attachment error; manual §8.5 rewritten. Gate tests rewritten to pin optionality — 286 tests in the WorkOrders/Attachments/Notifications suites green. | Frontend (done), Ops | ⚠️ Unacknowledged |
| 2026-08-18 | ✅ **D-022 resolved — mid-WO PM marking persistence.** The open question was whether a level marked during a work order takes effect immediately or is staged until close. **Staged**, as the working default proposed: `work_order_pm_marks` (unique per WO) records it, `CloseWorkOrder` applies it, cancel discards it. Applying immediately would advance an asset's schedule for work that may never be signed off. Shipped in Phase 6 (2026-08-17) and extended 2026-08-18 with the close tri-state — omitted applies the mark, an integer overrides it, an explicit `null` suppresses it. | Product, Frontend | ⚠️ Unacknowledged |
| 2026-08-17 | ✅ **External-review fixes — 19 findings, all five batches landed. Ships as ONE release with 4b; do not deploy partially.** **Two things were broken in production and are now fixed:** (1) the **parts CSV download** navigated to `/api/parts/export-csv` on the *SPA* origin — production is split-domain and the SPA host has no `/api` proxy, so the operator downloaded `index.html` renamed `.csv`; it goes through `api.download()` now. (2) **any asset `at_the_field` was uneditable** — the edit form posted the whole draft back including a status the API rejects as manual, so renaming a rig-deployed asset 422'd; the form now sends only changed fields. **API changes:** `POST /work-orders/{id}/close` — `serviced_pm_assignment_id` is now a **tri-state** read with `array_key_exists`: **omitted** = apply the staged PM mark, **integer** = override it, **explicit `null`** = apply none (new; audits `work_order_pm_mark.suppressed`). `PATCH /assets/{id}` discards a submitted `operational_status` when the location move derived one (audited `asset.status_payload_discarded`), applies the eligibility guard to its **location branch only** (withdrawn assets stay editable), and is now **one transaction** — a tag collision no longer leaves the asset already moved. `DELETE /attachments/{id}` **403s on a closed or cancelled work order**, for everyone including Admin. `DELETE /work-orders/{id}/pm-mark` 403s on a terminal work order. **R-1 and R-2 gain `condition_status`** — see the row below. **Also:** `ImportPartQuantities` no longer truncates rows to the header count (an unquoted `1,200` silently committed stock as **1**); six actions now lock the asset row before the eligibility guard (being inside a transaction is not holding the row); all report CSVs neutralise formula-triggering cells, and the parts import strips that guard before its code cross-check. `deploy.sh` **refuses to run** while a legacy `operational_status` survives. 1280 tests green, verified against 399 live dev assets. | Frontend (close payload tri-state — done), **Ops (deploy.sh gate + cutover doc reordered)** | ⚠️ Unacknowledged |
| 2026-08-17 | ✅ **Condition reporting — R-1 and R-2 (closes the last mandatory item of the vocabulary release).** **R-1 Assets Status:** new `condition_status` filter (accepts retired values too, or assets carrying them become unreachable); rows carry `condition_status` + `condition_label`; new `summary.by_condition` — **a list, not a keyed map** like `by_status`, because the vocabulary is Admin-editable and enumerating every configured value would show a permanent row of zeroes; includes an `Unrecorded` bucket with an empty `value` so counts sum to `summary.total`; CSV gains a **Condition** column. **R-2 Asset Distribution:** `condition` is a fourth **grouping dimension** (`group_by=condition`, combinable with the other three — `max` raised 3→4), plus a `condition_status` filter. Deliberately a dimension and **not** per-condition count columns: those would change the report's shape, and its CSV header, every time LDC add or retire a value. Null groups as **"Unrecorded"**, which is distinct from a condition of "Normal" — a gap in the data must not read as a clean bill of health. Labels come from one shared `AssetConditionLabels` so a report can never disagree with the asset page. | Frontend (done — filters on both reports, Condition column on R-1), Reporting | ⚠️ Unacknowledged |
| 2026-08-17 | ✅ **Phase 7 shipped — RQ3: parts CSV download + quantity upload.** New `GET /parts/export-csv` (whole catalogue, active and inactive, quantities at stored `numeric(14,3)` precision) and `POST /parts/import-quantities` (multipart `file`). **Administrator only** — new `PartPolicy::importQuantities`, deliberately narrower than `manage()`, which also admits Manager. **Matching is on `part_id`, never the Part No.** (Q8): `erp_part_code` is cross-checked to catch a spreadsheet lookup that slipped a row — blank skips the check, a filled mismatch rejects. **All-or-nothing**: one bad row returns 422 with line-numbered errors (first 40 plus `error_count`) and writes nothing. Success returns `{rows, updated, unchanged}` and audits one `parts.quantity_upload.completed` event carrying the file's SHA-256. Tolerates Excel's UTF-8 BOM and ignores extra columns, so the download re-uploads unedited as zero changes. Caps 5 MB / 25,000 rows. Row-locked, so a correction and a WO consumption serialise. ⚠️ **Interim** — `SyncParts` overwrites these wholesale once the live ERP feed lands (🟠 D-020); the dialog says so. **Manual §10.4a corrected** — it claimed matching was on Part No. 1243 tests green. | Frontend (done — Admin toolbar controls on Parts), Ops | ⚠️ Unacknowledged |
| 2026-08-17 | ✅ **Phase 6 shipped — RQ1: PM level marking during a work order.** New `PUT`/`DELETE /work-orders/{id}/pm-mark` (body `{asset_pm_assignment_id}`), authorization `updateExecution` — the assigned technician while open/in-progress, Manager/Admin until close. **PUT is idempotent**: `work_order_pm_marks` is unique per work order, so re-marking replaces rather than accumulates (the ladder is cumulative — L3 ⊇ L2 ⊇ L1 — so the picker is a single 'highest level performed' choice). **Staged, not applied**: the mark reaches the asset's schedule only at close, and **cancel discards it** — applying immediately would advance a schedule for work that was never signed off. At close an explicit `serviced_pm_assignment_id` **overrides** a staged mark and the difference is audited (`work_order_pm_mark.superseded`); a mark whose schedule was deactivated meanwhile is skipped, audited and warned about rather than blocking. ⚠️ **Cascade generalised**: `CloseWorkOrder` matched `/^L([1-4])$/`, so an `L5` rule silently cascaded to nothing — now any `L<number>`. Custom levels (e.g. `SEASONAL`) still cascade to nothing, now deliberately and stated in the UI. The 4b Need-Inspection close warning now also requires **no PM recorded**, so it stops firing on the correct case. `pm_mark` added to the work-order payload. 1213 tests green. | Frontend (done — picker in the execution section, close dialog pre-selects), Ops | ⚠️ Unacknowledged |
| 2026-08-16 | ✅ **Phase 5 shipped — RQ2: a work order needs an attachment before it can be CLOSED.** D5 answered, and the user corrected the plan's recommendation: the gate is on **close, not completion**. The technician marks the work finished, then uploads the paperwork; the manager cannot sign off until it is there. **API changes:** `POST /work-orders/{id}/close` returns **409** `This work order has no attachments. Upload the completed form or supporting document before closing it.` when nothing is attached — the guard runs before every mutation, so a refused close changes nothing. `POST /work-orders/{id}/attachments` now allows uploads on a **completed** work order (previously the SPA hid the control at completion) and returns **403** once closed or cancelled — the latter enforces what the user manual already claimed. **Cancel is deliberately not gated.** No mime allowlist: PDFs, spreadsheets and photos all accepted. ⚠️ **Presence check, not kind** — ATMS cannot tell an inspection form from a photograph; expressing that would need an attachment category that does not exist. New shared test helper `TestCase::attachToWorkOrder()`. 1194 tests green. | Frontend (done), Ops | ⚠️ Unacknowledged |
| 2026-08-16 | ✅ **Q7 answered: NO — R-11 is cancelled, and every LDC question on the vocabulary programme is now closed.** LDC do not want a withdrawn / out-of-service assets report, so **Phase 8 has no content** and 🟠 D-021 is retired (it was deferred pending this answer). No code was written for it. **If it is ever revived:** a plain count of deactivated assets is trivial, but a report **grouped by reason** (lost in hole / damaged beyond repair / scrapped / disposed) is **not recoverable** — those labels had no home in the agreed vocabulary, `maintenance_sub_status` never held a value (400/400 NULL), and nothing records *why* an asset was retired. That version needs a withdrawal-reason field added first and populated going forward; already-retired assets would carry no reason at all. | Product, Reporting | ⚠️ Unacknowledged |
| 2026-08-16 | 🚨 **Release 4b shipped — the status-vocabulary switch. REQUIRES A MAINTENANCE WINDOW: follow `docs/RELEASE-4b-CUTOVER.md`, NOT `deploy.sh`.** New code against un-migrated data throws on every read of an affected asset, so migration and deploy must not overlap. **`operational_status` is now 4 values**: `ready_for_field`, `under_maintenance`, **`failure`** (was `down`), **`at_the_field`** (derived from location, rejected by every manual picker with 422). `scraped`/`under_inspection`/`lih` are gone — an asset that has left the fleet is **deactivated**. **API changes:** `POST /work-orders/{id}/close` **no longer accepts `asset_status`** (always `ready_for_field`) and returns a `warnings` array; cancel's `asset_status` is now `failure|ready_for_field`; `/work-orders/{id}/asset-status` accepts only the 3 manually-selectable values; `PATCH /assets/{id}` and `POST /assets/{id}/location` **409** on a manual move of a `failure`/`under_maintenance` asset, and allow an `at_the_field` asset only back to a yard/building; `POST /maintenance-requests/{id}/approve` gains optional `move_to_location_id`; `AssetResource` gains `condition_status`/`condition_label`/`operational_status_label` and **drops `maintenance_sub_status` and `erp_status`**; R-10A returns 4 keys; `by_status`/`by_operational_status` rename `down→failure` and `scraped|under_inspection|lih → at_the_field`; CSV headers follow. **Workbook import** maps `down→failure` and **rejects** the three retired values. 1181 tests green. | Frontend (done in this release), **Ops (cutover doc — read before deploying)** | ⚠️ Unacknowledged |
| 2026-08-16 | ✅ **Review hardening — pre-4b cleanup (no feature work).** An independent review of Phases 0–4a found zero critical issues and no scope creep; this closes its three test gaps and three real defects. **API change:** `POST /assets/{asset}/bookings` now rejects an asset **withdrawn from maintenance** as well as a deactivated one (409, cause-distinct message) — booking joins the shared `AssetWorkEligibility` guard, closing a hole where `Asset::booted` released a withdrawn asset's bookings and nothing stopped the next request re-creating one. Message on the deactivated axis changed from `Cannot book an inactive asset.` to `Cannot create a booking for a deactivated asset.` **Also:** the master-data default-deactivation 422 no longer tells Admins to "make another value the default first" (no endpoint accepts `is_default` — see 🟠 D-023); the `asset_conditions` seed `down()` no longer deletes Admin-added values; dead `ToggleAssetBooking` deleted (unrouted, and wrote to `is_booked`, which is a derived accessor, not a column). **New tests:** stock-movement audit payloads, `MasterDataItemTest` (allowlist 404 / `value` immutability / default protection / authorization), `AssetConditionDefaultTest` (default resolution on both creation paths). 1130 tests green. | Frontend (booking 409 message text), Ops | ⚠️ Unacknowledged |
| 2026-08-16 | ✅ **Phase 4a shipped — additive vocabulary schema (safe with old code deployed).** ⚠️ **`operational_status` values are UNCHANGED** — 4a adds, it does not rewrite. **`OperationalStatus` now has 8 cases**: the 4 agreed values (`ready_for_field`, `under_maintenance`, **`failure`**, **`at_the_field`**) alongside the 4 legacy ones, so the enum can deserialize existing `down`/`scraped` rows while accepting new values during the rollout. Use **`OperationalStatus::current()`** for anything that offers a choice — a picker must not show a value 4b deletes. **New:** `assets.condition_status` (nullable string, indexed, **no enum cast** — Admin-editable vocabulary); `master_data_items.is_default` + partial unique index `UNIQUE (group_key) WHERE is_default`; `asset_conditions` seeded (Normal ⟵ default, Need Assembly, Missing Parts, Need Inspection) and all 400 assets backfilled to `normal`. **API changes:** `GET/POST /admin/master-data/{groupKey}` now **404s** for any group outside `MasterDataItem::MANAGED_GROUPS`; `PATCH /admin/master-data/items/{item}` **no longer accepts `value`** (immutable — other tables store it; rename `label` instead) and **422s** when deactivating the default row; R-10A (`/reports/asset-status-distribution`) now returns **8 status keys** instead of 6, dropping back to 4 after 4b. Rollback proven against live data. 1108 tests green. | Frontend (R-10A key set; use `current()` for pickers), Ops | ⚠️ Unacknowledged |
| 2026-08-16 | ✅ **Phase 3 shipped — parts stock decrement / restore (Q6).** Recording a part on a WO now **subtracts** `available_quantity`; removing the line **restores** it. Previously an untouched ERP snapshot. **API changes:** `POST /work-orders/{id}/parts` quantity validation is now `['required','numeric','min:0.01','regex:/^\d+(\.\d{1,2})?$/']` — **max 2 decimal places**, and zero/negative are 422 (were previously accepted or silently rounded); a request exceeding stock returns **409 `Insufficient stock: only N.NNN available.`** (new, distinct from the existing out-of-stock 409); `available_quantity` on every part payload is now a **live balance**, so any frontend copy calling it an "ERP snapshot" is stale. **Precision:** `RecordWorkOrderPart::execute` takes `string $quantity` (was `float`) and both stock writes are raw SQL against the numeric column — no PHP arithmetic on a balance. **Concurrency:** the part row is locked before the guard reads it, so two requests for the last unit serialise. **Audit:** both events carry `available_quantity_before`/`_after`. ⚠️ **Interim only — see 🟠 D-020:** ERP remains the authority and the weekly sync overwrites the column wholesale once `LDC_ERP_PARTS_API` is set. 1101 tests green. | Frontend (validation limits + new 409 + stale "snapshot" wording), Ops | ⚠️ Unacknowledged |
| 2026-08-16 | ✅ **Phase 2 shipped — RQ4: `erp_part_code` exposed and searchable everywhere.** Was Admin-only and deliberately withheld; it is the "No." column LDC's Maintenance team works from, so **every role now receives it**. **API changes:** `PartResource` returns `erp_part_code` in the base shape for all roles (`erp_raw_data` stays Admin-only); `PartIdentityResource` gains `erp_part_code`, so it appears on **every embedded part** — WO part lines, pickers, cards, the consumption report's nested part; `GET /parts?search=` now matches the ERP code. **`WorkOrderPartResource` no longer merges `erp_part_code` on top of the identity shape** (it did so only because the shared shape withheld it) — the emitted JSON is unchanged, but frontend `WorkOrderPart.part` is now plain `PartIdentity`, no intersection. **Frontend:** new first badge `identity-badge-part-code` (mono, shares the part-number treatment), sortable **"Part No."** column on the Parts table, detail field, read-only edit-sheet field, combobox placeholder, Add Part dialog widened to `dialog-md`. 1092 tests green, type-check + build clean. | Frontend (new field on every part payload), Ops (deploy — frontend rebuild required) | ⚠️ Unacknowledged |
| 2026-08-16 | ✅ **Phase 1 shipped — unified asset-eligibility guard (`App\Support\Assets\AssetWorkEligibility`).** `is_active = false` now blocks new maintenance work exactly as `maintenance_status = withdrawn` does, across **eight** surfaces: MR create (422), MR approve corrective **and** preventive (409), WO assign (409), WO start (409, new), direct PM evaluation (409, new), the evaluate-all batch, `AssetPmAssignment::scopeEvaluable` (the scheduler — the widest hole; the daily 06:00 job had been raising PM requests for deactivated assets), and asset location change. `UpcomingPmReportQuery` (R-1) mirrored so the forecast matches what the scheduler will actually raise. **API changes:** ① `POST /assets/{id}/location` on an ineligible asset now returns **409, was 422** — frontend is unaffected because `ApiError.validationErrors` needs 422 *and* an `errors` key and that payload never had one, so both old and new land on `e.message`; ② three guard messages reworded to be **cause-distinct** ("…for an asset withdrawn from maintenance." vs "…for a deactivated asset.") — the old text said "inactive asset" for a *withdrawn* asset, naming the wrong switch. **Start guard only** — complete/close/cancel deliberately untouched so work already under way on a since-deactivated asset can still be finished. 1092 tests green. | Frontend (message text + the 409), Ops | ⚠️ Unacknowledged |
| 2026-08-15/16 | ⚠️ **Status vocabulary — 🔴 In Progress.** Design agreed 2026-08-16; **Phase 0 (doc hygiene) and Phase 1 (eligibility guard) are done**, Phases 2–8 pending. Design: `docs/plans/2026-08-07-operational-status-vocabulary.md`; execution: `.kilo/plans/2026-08-16-status-vocabulary-implementation.md`. | Backend, Frontend, Ops | 🔴 Phases 0–1 done, 2–8 pending |
| 2026-08-05 | **Repair / Service vocabulary — display only, no API or DB change.** `mrTypeLabel` returns **Repair / Service** (was Corrective / Preventive), propagating to the MR list Type column + filter, MR detail subtitle, asset maintenance history, and WO detail subtitle. `PM Rules (Service)` on the Admin tab and asset PM card; `New / Create Maintenance Request (Repair)` on the create sheet and dialog. ⚠️ **Nav and list titles deliberately unchanged** — the MR list holds both kinds, so a bracketed title would misdescribe half its rows; the distinction lives on the per-row Type value instead. `corrective`/`preventive` remain the domain terms everywhere in the API, DB, and `MrType`. Audit-log filter labels untouched (they name backend event families). Frontend-only — needs rebuild/deploy. | Frontend, Ops (deploy) | ⚠️ Unacknowledged |
| 2026-08-05 | **⚠️ ADDITIVE API + 2 migrations — stored delta, meter snapshot at close, service-on-repair.** (1) **`asset_meter_readings.entered_delta`** (nullable): `POST /assets/{id}/meter-readings` accepts it, `AssetMeterReadingResource` exposes it. Informational only — nothing in PM, guards, or reports reads it. **`UpdateMeterReading` clears it when `reading_value` changes** (the edit dialog posts absolutes; a stale delta would stop matching). (2) **New `work_order_meter_snapshots` table** — closing snapshots the asset's meter **per reading type**; exposed as `meter_snapshots` on the WO **detail** endpoint. Per type because three are live (Hours, Km, Depth) and a single column pair would silently lose two. Snapshots are **immutable** — not recomputed when a source reading is edited. This is what finally makes "usage since the last repair" answerable; "since last service" was already derivable and is now exposed as **`usage_since_last_service`** on `AssetPmAssignmentResource`. (3) **`POST /work-orders/{id}/close` accepts `serviced_pm_assignment_id`** — declares a service performed alongside a repair: resets that schedule's date + reading baselines, cascades to lower levels, and **cancels any pending PM request** for it (409 if the assignment is inactive or belongs to another asset). The suppression is written `decision_type = 'performed_under_repair'`, **not** `cancelled`, with its window derived from the new baseline, so PM compliance does not read it as a skipped service. `CancelMaintenanceRequest` gained an optional trailing `$decisionType` (default `cancelled`). ⚠️ **`CloseWorkOrder` ordering is load-bearing:** confirm readings → preventive-PM baseline block → declared corrective service → snapshot — the PM block's **two** latest-confirmed-reading queries must run before the service declaration resets baselines. Suite **1073 green**, Pint clean, workers restarted, verified live. | **Frontend**, Backend, Ops (deploy + migrate + `docker compose restart queue scheduler`) | ⚠️ Unacknowledged |
| 2026-08-04 | **Closing a work order now confirms the readings taken on it — no API contract change.** New `ConfirmWorkOrderReadings` action, called by `CloseWorkOrder` immediately after the status transition. Readings with `work_order_id = <this WO>` and `confirmed_at IS NULL` are confirmed **oldest-first** through the existing `ConfirmMeterReading`; a reading failing a monotonicity guard is **skipped, not fatal** — the close always succeeds — and the skip is audited as **`meter_reading.confirm_skipped`** (new event, added to `auditColumns.ts`). **Decision: verification is a by-product of close, not a button.** Close is Admin/Manager only and the technician who took the reading cannot do it, so close is a real second pair of eyes; a Verify button would be clicked by the same person who entered the value. ⚠️ **Do not reopen as "add a Verify button".** ⚠️ Three invariants: ascending `reading_at` order (else each reading strands the one below it), skip-don't-abort (a bad reading must never block a close; safe because `ConfirmMeterReading`'s nested `DB::transaction` is a savepoint), and placement **before** the PM block (there are **two** latest-confirmed-reading queries in `CloseWorkOrder` — the assignment's own and one inside `resetLowerLevelAssignments`). Effect: readings stop accumulating in a permanently unusable state, and R-20 / Asset Usage get data for the first time. Suite **1059 green**, Pint clean, workers restarted. Deferred: D-015 (requester MR readings), D-016 (cancel opt-in), D-017 (manual), D-018 (first-reading delta bug). | Backend, Frontend (informational), Ops (deploy + `docker compose restart queue scheduler`) | ⚠️ Unacknowledged |
| 2026-08-04 | **PM rules are date-only now — reading triggers parked (D-014).** `PmRuleForm.vue` `TRIGGER_OPTIONS` reduced to "Calendar (date-based)", so new PM rules can no longer be created as Usage or Whichever-comes-first. Root cause that drove the decision: readings can only be recorded on the WO page and **no UI ever calls the confirm endpoint**, so confirmed readings never exist and reading-based PM could never fire. **Nothing deleted** — backend API still accepts `reading`/`date_or_reading`, calculator reading logic and existing rules' reading config are untouched, and existing `date_or_reading` rules keep working on their date side. Frontend-only — needs rebuild/deploy. Re-enablement procedure in `docs/plans/2026-08-04-pm-reading-trigger-parking.md`. | Frontend, Backend (informational), Ops (deploy) | ⚠️ Unacknowledged |
| 2026-08-03 | **Admin table visual fixes (PM Rules + WO Forms).** (1) Centered columns had left-aligned headers — `.app-data-table-thead th` (specificity 0,1,1) out-ranked `.app-data-table-th-center` (0,1,0); the center rule is now scoped as `.app-data-table-thead th.app-data-table-th-center`. This also fixes the same latent misalignment on **every centered column app-wide** (Active columns etc.). (2) Template column widened to 320 px on both tables (was 180/200). `vue-tsc` clean. Frontend-only — needs rebuild/deploy. | Frontend, Ops (deploy) | ⚠️ Unacknowledged |
| 2026-08-03 | **Index `per_page` cap raised 100 → 5000 on all six capped endpoints** (assets, parts, employees, pm-rules, wo-forms templates, asset maintenance-history) — now uniform with work orders/MRs. The frontend's fetch-all cursor walk previously cost one VPS round trip (~300–400 ms, mostly TLS/network) per 100 rows: Assets loaded in 4 sequential requests (>1.4 s), Parts in 8 (~2.1 s). Both now arrive in **one request (~350 ms)**. No API contract change; DB query itself measured at 5–7 ms. New tests: `AssetIndexPaginationTest`, `PartIndexPaginationTest`. Full suite 1046 passed, Pint clean. Committed `185c7c5`, deployed — verified live: assets load in one 22.5 kB request (~0.7 s, was >2 s). | Frontend (informational — no action), Ops (deploy) | ⚠️ Unacknowledged |
| 2026-08-02 | **Phase 1 tidy-up (user decisions).** (1) `AssetLocationUpdateView.vue` **deleted** — D-007 ✅. (2) **`operational_status = 'inactive'` now displays as "Retired"** — display-only, no migration/API change: badges, report filters, dashboard legend, catalogue copy, WO/asset selects, manual (§5.9/§6.4/§9.1/R-10A/Appendix A). `is_active = false` keeps "Inactive". (3) **No activity feed** — user ruled it out; Recent asset moves is the final dashboard closing column. (4) All Assets table Status column now shows **`operational_status`** (was `maintenance_status` — 400/400 `enrolled`, zero information). | Frontend, Backend (informational) | ⚠️ Unacknowledged |
| 2026-08-01 | **⚠️ BREAKING — `POST /work-orders/{id}/start` can now refuse.** It accepts an optional **`location_id`**, and **409s** unless the asset is already at a `workshop` or `yard`. A supplied `location_id` must itself be an active workshop/yard (409 otherwise) and is applied as a **real recorded transfer** (`asset_location_histories` row, reason `Started work order WO-xxxx`, `asset.location_updated` audit) in the same transaction as the start. Rationale: nothing in the WO lifecycle ever touched `current_location_id`, so assets were recorded as being repaired on rigs — and `AssetDeployment` counts `rig`/`well_site` as **DEPLOYED**, i.e. the dashboard counted them as earning while in pieces. ⚠️ **The move is permitted to anyone who may start the WO, technicians included** — `AssetPolicy::updateLocation` is deliberately not consulted here; `POST /assets/{id}/location` keeps its narrower admin/manager/logistics policy. ⚠️ **396 of 400 live assets had no location at the time, so in practice every start hit the picker** *(resolved 2026-08-04 — the TJB relocation placed all 400)*, not just the rig case. Also additive: `WorkOrderResource.asset` gained **`current_location {id, name, code, type}`** (`type` included because the Start button routes on it). | **Frontend**, Backend, Ops | ⚠️ Unacknowledged |
| 2026-08-01 | **Meter readings can be attributed to a work order — additive.** `asset_meter_readings` gained a nullable **`work_order_id`**; `POST /assets/{id}/meter-readings` accepts it (422 if that work order belongs to a different asset) and `AssetMeterReadingResource` exposes it. Existing rows stay null and read as asset history. The WO page previously listed **every** reading the asset had ever had with no way to tell which belonged to the job on screen; it now splits "Recorded on this work order" from a collapsed "Asset reading history". `RecordMeterReading`'s new `$workOrderId` is **appended last** — `$maintenanceRequestId` sits at position 7 and callers pass positionally. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **🐛 `workshop_yard` removed as a selectable location type.** It was offered by `LocationForm.vue` and seeded by `LocationSeeder`, but is not a `LocationType` case — so any location created with it returned `null` from `AssetDeployment::forLocationType()` and dropped out of every utilisation figure, and would have failed the new work-order start guard. Seeder corrected to `yard` (what the prod baseline migration and live DB already hold for that row). The `displayHelpers` label is kept so any legacy row still renders. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **Close asks for the asset's next operational status.** `POST /work-orders/{id}/close` accepts optional `asset_status` (`in:down,active`; **absent = `active`** — existing callers unchanged). The close dialog gained "Asset status after close": pre-seeded **Active** / **Down** — only these two, no Inactive option; the never-un-retire-`inactive` guard is unchanged. 5 new tests; full suite **1042 green**; committed `1d94767`. ⚠️ **Investigation finding:** close *already* auto-reverted to `active` (audit-proven) — an asset can still read `down` after a close because a **second, still-open WO** on the same asset set it down; nothing in the UI explains that yet (P2-010 will remove the possibility). | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **🐛 Three defects fixed after the category work went into use.** (1) **PM rule coverage was wiped by editing a rule from the list** — `PmRuleIndexQuery` did not eager-load `maintenanceCategories`, so the edit sheet opened empty and posted `[]`. ⚠️ **"L3 Maintenance Motors" lost its categories this way and must be re-assigned by hand.** Rule: any list feeding an edit form must load every relation that form submits back. (2) **A non-modal sheet leaves the table behind it clickable** — clicking Edit on a second row swapped the record while the form kept the first one's values, writing them over it on save. Both the WO Form and PM Rule sheets now re-initialise when the edited record's identity changes. (3) `AppDataTable` global search ignored **array** column values, so the new Maintenance Categories column matched nothing. Also: selected categories now pin to the top of the category picker, and two `<Label for>` ids in `PmRuleForm` pointed at elements that never existed. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **⚠️ OPERATIONS — restart `queue` and `scheduler` after any job or Action change.** Long-running workers hold PHP classes from boot. Every `ReconcilePmCategoryAssignmentsJob` failed for ~2 hours against a worker booted before the job existed (`Undefined constant …PM_CATEGORY_RECONCILE`), so PM rules showed category coverage while creating **no assignments at all** — and nothing surfaced in the UI. Restart + re-dispatch produced 515 assignments. A full `docker compose up -d` after a build is safe; editing against a running stack is not. Documented in `docs/OPERATIONS.md`. | Backend, Ops | ⚠️ Unacknowledged |
| 2026-08-01 | **⚠️ BREAKING — WO Forms route by Maintenance Category (D-011 BUILT).** `FormTemplateResource.fa_subclass_code` → **`maintenance_categories: [{id, code, name}]`**; `POST`/`PATCH /admin/wo-forms/templates` now take **`maintenance_category_ids[]`** (required, min 1 on create); list filter `?fa_subclass_code=` → **`?maintenance_category_id=`**. **The 4 `/admin/fa-subclass-type-codes` CRUD routes + controller are DELETED** (404 now) — `/list-options/fa_subclass_type_codes` is a different controller and stays for report filters. A form may serve **several** categories; at most **one active** form per category, enforced by a partial unique index on the pivot, with a 422 on create and a 409 on reactivate/edit naming the colliding category and the form holding it. ⚠️ **The 2 existing templates migrated DEACTIVATED with no categories** — there was no subclass→category mapping to migrate through; an Admin must reassign them, and reactivation is refused until they have at least one category. Coverage is now editable after creation. `atms:import-assets` no longer rejects unknown FA subclass codes; it records them with type code `UNK`. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **⚠️ `assets.maintenance_category_id` is NOT NULL, defaulting to a seeded `UNCLASSIFIED` category (P1 BUILT).** `PATCH /assets/{id}` with `maintenance_category_id: null` now **422s** — a category cannot be cleared, only reassigned (to Unclassified if genuinely unknown). **Reports: the "Uncategorised" bucket is now unreachable for assets** — an unclassified asset appears as the real `UNCLASSIFIED` category, so it *is* counted in `summary.total_groups` and *is* filterable, and `is_unassigned` is never true for the maintenance-category dimension. Parts keep a nullable category, so their null handling is unchanged. The sentinel exists because the ERP sync creates assets with no category and no mapping to infer one from; it cannot be deactivated. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **PM Rules can cover Maintenance Categories (D-012 BUILT) — additive API.** `POST`/`PATCH /pm-rules` accept **`maintenance_category_ids[]`**; `PmRuleResource` gained `maintenance_categories`; `AssetPmAssignmentResource` gained **`origin`** (`manual`\|`category`) and `source_maintenance_category`. Covering a category expands into one assignment per member asset **in a queued job**, so the response returns before the assignments exist — reload to see them. Expansion follows assets automatically (category change, deactivation, withdrawal, workbook import). **Reconciliation never reactivates an assignment a person deactivated**, and never touches a `manual` row. `asset_pm_assignments.assigned_by` is now **nullable** — null means reconciliation, not a person. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **PM evaluation no longer scales with the register (D-013 BUILT).** No API change. `EvaluatePmRulesJob` now chunks and fans out `EvaluatePmAssignmentsJob` per chunk; due-ness is checked before the transaction, so the row lock is paid only for assignments that look due. Query count is now **flat** — identical at 5 and 25 assignments, verified by test. `POST /pm-rules/evaluate-all` was chunked through the same runner and keeps its `{evaluated, generated}` shape. | Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **⚠️ R-2 row shape changed AGAIN — multi-dimensional grouping.** `group_by` is now an **array**: `?group_by[]=maintenance_category&group_by[]=size&group_by[]=location` returns one row per distinct combination (82 rows over the live 400 assets). A bare `group_by=location` string still works. **Row shape: `group_key`/`group_label` → `groups: [{dimension, key, label, is_unassigned}]`**, one entry per requested dimension, **in the order requested** — selection order is column order. `summary.total_groups` counts only rows with no null bucket. `DashboardView` reads `groups[0]`. Also: `api.ts buildUrl()` now serialises arrays as `key[]=a&key[]=b` (previously stringified to `"a,b"`) — shared client change, additive. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **`/reports` now lists all 20 live reports** (was 7 Pass-1 only, leaving 13 built reports reachable by URL alone). `mustTierThemes` deleted from `useReportCatalog`. ⚠️ **`/reports-verification` is now redundant and can be deleted** — it existed solely to show the full catalogue. **Export CSV button** added to the 3 reports whose endpoint supports it (Assets Status, Asset Distribution, Most-Used Assets) via new `api.download()` + `useReportCsvExport`; export sends the **applied** filters, not current form state. | Frontend | ⚠️ Unacknowledged |
| 2026-08-01 | **🐛 Table header/data misalignment fixed across all 14 reports with numeric columns.** `.report-table thead th` (0,1,2) outranked `.report-table-num` (0,1,0), so numeric **headers** stayed left-aligned over right-aligned figures. Fixed by qualifying to `.report-table th.report-table-num` (0,2,1). Column counts and numeric-class positions audited across all 21 report tables — clean. Also: the **asset picker now sits last and grows** (`.report-filter-asset`) in all 5 reports that have one; at the old 11rem width both the input and its dropdown truncated, since the panel inherits the trigger width. | Frontend | ⚠️ Unacknowledged |
| 2026-08-01 | **⚠️ BREAKING report contracts — FA Subclass removed from all reports.** Filter param `fa_subclass_code` (string) → **`maintenance_category_id`** (int) on 7 reports. `group_by=asset_class` now **422s** on MTBF/MTTR/Bad-Actor — the dimension is gone. Response fields renamed: `AssetStatusReportItem` **dropped** `fa_subclass_code`; `PartsConsumptionItem.asset_class` → **`asset_maintenance_category`**; `FormResultRow.asset.asset_class` → **`asset.maintenance_category`**. Rationale: FA Subclass is ERP-written, so it cannot drive an ATMS report. **Asset detail API is unaffected** — `AssetResource` still exposes `asset_class`/`fa_subclass_code`. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **R-2 generalised → Asset Distribution.** `GET /api/reports/asset-distribution?group_by=location\|maintenance_category\|size` (default `location`). ⚠️ **Row shape changed:** `location_id`/`location_name` → **`group_key`/`group_label`**, `summary.total_locations` → **`total_groups`**, plus new `group_by` in the payload. Old API path `/reports/assets-by-location` and SPA route `/reports/assets-by-location` both still resolve as aliases. Frontend files renamed to `useAssetDistributionReport.ts` / `AssetDistributionReport.vue`; **DashboardView consumes this report** and was migrated. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **R-22 Most-Used Assets BUILT.** New `GET /api/reports/asset-usage` + page at `/reports/asset-usage`. Ranks assets by accumulated usage against **one** reading type (Operating Hours / Kilometer Driven / Depth — units never mix; `reading_type.unit` travels with the payload). `group_by=asset\|maintenance_category\|size`, top-N `limit`, summary spans all assets not just shown rows. **Usage is a difference, not a sum** — meters are cumulative, so baseline is the last confirmed reading *before* the window (falling back to the window floor for newly-metered assets). Only confirmed readings count. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-08-01 | **CSV export live on 3 reports.** `?format=csv` on the **existing** report endpoint (no separate `/export` route), so filters/auth/sort are provably identical to the on-screen table. UTF-8 BOM for Excel + Arabic; timestamps in `Africa/Tripoli`; streamed, so an export is the whole result set not one page. Wired: `asset-status`, `asset-distribution`, `asset-usage`. **18 reports still JSON-only, and no Export button exists in the UI yet.** | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-07-31 | **R-1 Assets Status Report BUILT + reports index renamed.** New `GET /api/reports/asset-status` (cursor-paginated, `{data, summary{total,by_status,booked}, meta}`) and page at `/reports/asset-status` — LDC's "Report 1", the only listing in an otherwise analysis-only catalogue. Filters: location, operational_status, asset_kind, booked, from/to + `date_field` (`updated_at` default, `created_at` optional). ⚠️ **Two pinned interpretations, stated in the UI and awaiting LDC confirmation:** the date range filters created/updated timestamps and returns *current* status (no status-history table exists — see D-009), and **"Assigned To" is the technician on the asset's open work order** (assets have no custodian column). **Routes renamed:** `/reports` → `ReportsView.vue` (final), `/reports-real` → `/reports-verification` + `ReportsVerificationView.vue` (admin-only, delete after sign-off). **Catalogue entries R-10B and R-11 removed** — under the 2026-07-31 "ERP-owned" decision, now superseded 2026-08-16 (the design drops those values from the vocabulary entirely — R-10B not restorable, R-11 only as a simplified withdrawn count if LDC asks); R-12 correctly remains deferred (Phase 2 assembly, not ERP). 946 tests pass, Pint + `vue-tsc` clean. **No export yet — D-010 is next.** | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-07-31 | **Dashboard BUILT — `/dashboard` is final, and `GET /api/dashboard/kpis` gained two keys.** New `kpis.utilisation` (percentage, eligible, deployed_eligible, by_bucket{deployed,idle,maintenance}, unlocated, unclassified, booked, total) and `kpis.readiness` (total + pm_coverage/location_recorded/baseline_reading, each {covered, percentage}). `kpis.asset_health` additively gained `by_booking` — **additive only, nothing removed, no consumer breaks.** ⚠️ **Superseded 2026-08-16:** the 2026-07-31 "withdrawal is ERP-owned / never surfaced, counted or reported" decision is obsolete — the final design drops LIH/DBR/Scrapped from the vocabulary; `is_active`/`withdrawn` carry the meaning. Code still implements the old decision (a `by_maintenance_status` key was built and then removed; R-10B/R-11 dropped from the catalogue) until the vocabulary design ships. The `enrolled` filter remains an internal population guard only. New `App\Enums\AssetDeployment` is the **single source of truth for deployed-vs-idle** (rig+well_site = deployed; yard+building = idle; workshop = maintenance) — change it there and nowhere else if LDC redefines it. Components renamed: `/dashboard` → `DashboardView.vue` (final), `/dashboard-real` → `/dashboard-verification` + `DashboardVerificationView.vue` (admin-only, delete after sign-off). New `components/ui/segmented-bar` primitive. 933 tests pass, Pint clean, `vue-tsc` clean for all dashboard files. ✅ **2026-08-02 decision: no activity feed** — Recent asset moves is the final closing column; do not build an audit-log endpoint for it. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-07-31 | **Frontend routes changed.** `/locations2` and `views/locations/LocationsView.vue` **deleted** (legacy tabbed Locations view; `/locations` → `LogisticsLocationView` is unaffected, `ManageLocationsView` still used). `/dashboard-real` and `/reports-real` now carry `meta: { requiresAdmin: true }` — they previously had **no guard**, so they shipped in the production bundle and any authenticated user could reach them by URL. Both are internal-verification only and must never reach the client product. ⚠️ **`views/locations/AssetLocationUpdateView.vue` is now orphaned** — decision needed (see 🟠 D-007). `vue-tsc` clean. | Frontend | ⚠️ Unacknowledged |
| 2026-07-31 | **`CLAUDE.md` rewritten; `backend/CLAUDE.md` + `frontend/CLAUDE.md` removed — one root file now.** Rebuilt from live code only. Also: **git history was reset by the user** — single `Initial commit`, nothing prior is recoverable, no diff baseline. Verified baseline on that commit: backend **911 passed (2666 assertions)**, frontend `vue-tsc --build` clean. | Backend, Frontend | ⚠️ Unacknowledged |
| 2026-07-11 | **Docs reconciliation (Phase 1).** Closed out G-03 (location picker) and G-11 (relocated-assets widget) across `PHASE_1_GAP_ANALYSIS.md`, `TDL.md`, `CLAUDE.md` — both shipped in `de85abe` but were never marked closed. No code change. | Backend, Frontend (informational) | ⚠️ Unacknowledged |
| 2026-07-05 | **Dropped `maintenance_requests.type` column — `is_preventive` is the single stored discriminator.** `type` is now **derived** in `MaintenanceRequestResource` / `MaintenanceHistoryResource` (`is_preventive ? 'preventive' : 'corrective'`). **API contract unchanged**: both `type` and `is_preventive` are still emitted; `?type=preventive\|corrective` list filter still works (translated server-side to `is_preventive`). No frontend action required. Frontend team had pre-confirmed they can drop `is_preventive` from the TS interface and key off `type === 'preventive'` — safe to do so now. Migration `2026_07_05_000000_drop_type_from_maintenance_requests_table` applied to live Postgres. Full suite 483 passed. | Frontend (informational — no action needed), Backend | ⚠️ Unacknowledged |
| 2026-06-28 | VPS Frontend Issue Tracker created (`docs/atms/04-frontend/VPS_FRONTEND_ISSUES.md`) — 8 issues logged (5 MR, 3 WO, 1 Asset). 4 frontend fixes verified on VPS, 3 need backend follow-up. | Frontend, Backend | ⚠️ Unacknowledged |
| 2026-07-25 | **Operational MR/WO email notifications built — supersedes the 2026-07-11 "activation + reset only" scope.** 8 workflow notifications dispatched from the owning Actions (MR submitted/approved/rejected; WO assigned/started/completed/closed/cancelled). `AccountEmailTransport::send()` signature **changed** to a single `array $message` (`{ to[], cc?[], subject, templateData }`) — breaking for any other transport implementation. New `AccountEmailNotification` trait carries the shared mailbox lock + retry policy. 16 notification + 97 MR/WO tests green, Pint clean. **Still `ACCOUNT_EMAIL_TRANSPORT=fake` everywhere — nothing is delivered.** Hardening pass (R-007) done the same session: BCC default removed; new `FRONTEND_URL` + `App\Support\FrontendUrl` for all 10 email link sites (incl. activation/reset, which had the same wrong-host bug); all notifications moved to `ShouldQueueAfterCommit`. **Cc routing decision revised — the 2026-07-04 plan is withdrawn and the as-built routing accepted.** PM evaluation stays silent by decision. Full suite 679 passed. Docs updated: `PRODUCT.md` §Notifications, `ENGINEERING.md`, `OPERATIONS.md` §Email delivery, `ROADMAP.md`, `IMPLEMENTATION_HISTORY.md`, `README.md`, `CLAUDE.md`. **Also fixed (R-008): the test suite had been running as `APP_ENV=local` on the `database` queue driver** — `phpunit.xml` now needs a forced `<env>` *and* a `<server>` twin per value, because Laravel's `Env` reads `$_SERVER` before `getenv()`. Queued jobs now actually execute in tests; suite unchanged at 679 passed. | Backend (contract change + test env), Frontend (informational) | ⚠️ Unacknowledged |
| 2026-07-11 | **Email transport decision confirmed: Microsoft Graph `sendMail` only; Power Automate retired.** Azure app provisioned + `Mail.Send` consented + probe passed (HTTP 202). Graph transport **built and committed `618a8fe`**. Scope limit to activation + password reset is **superseded by the 2026-07-25 row**. Pre-release still open: final frontend URL, Application Access Policy, real user emails, production credential, queue-worker verification. Design notes archived at `docs/_archive/2026-07-13/legacy/03-backend/NOTIFICATIONS.md`; current behaviour lives in `docs/PRODUCT.md` / `ENGINEERING.md` / `OPERATIONS.md`. | Backend | ✅ Superseded 2026-07-25 |
| 2026-07-11 | **Asset API location filter correction documented.** `GET /api/assets?location_id={id}` now maps to the persisted `current_location_id` column in `AssetIndexQuery`; regression tests cover selected-location filtering and requester active-asset scoping. Verification is pending delivery-team test run. | Backend | ⚠️ Unacknowledged |
| 2026-07-11 | **G-09 Effective Date UI mismatch closed.** Removed the disabled/non-submitted datetime control from `UpdateLocationSheet`; immediate Phase 1 moves continue to use backend `effective_at = now()`. Updated navigation, screen inventory, component, form-requirement, and gap-tracking docs. Frontend type-check and production build pass. | Frontend, Backend (informational) | ⚠️ Unacknowledged |
| 2026-07-04 | **Self-service password change — `POST /api/auth/change-password`** (committed `a03b078`). Authenticated, no current-password required, invalidates all sessions/tokens, audits `user.password_changed`. 7 tests; full suite 483 green. | Frontend, Backend | ✅ Acknowledged (FE UI shipped in `77d4e13`) |
| 2026-06-28 | WO assignable roles expanded — Admin/Manager can assign WO to Technician OR Maintenance Manager (small teams, overloaded tech). | Frontend (assign picker), Backend (policy) | ⚠️ Unacknowledged |
| 2026-06-24 | ERP connection confirmed (Entra ID → BC OData V4). Token + assets working. Env vars in `backend/.env`. | Frontend (API base URL pattern changed), SM team | ⚠️ Unacknowledged |
| 2026-06-24 | Asset Assembly model decided. 5 API endpoints defined: `install`, `remove`, `swap`, `assembly-history`, `children`. | Frontend (new routes), Backend (new actions) | ⚠️ Unacknowledged |
| 2026-06-24 | Asset tag format `L-BBB-CCC-XXXX` decided. New `asset_tag` column added to spec. | Frontend (create/edit forms, QR), Backend (migration, validation) | ⚠️ Unacknowledged |
| 2026-06-24 | Mock ERP deprecated. Real ERP auth + URL pattern documented in `ERP_SYNC.md`. | Backend (4 PHP files to clean up) | ⚠️ Unacknowledged |
| 2026-07-02 | Admin Lists & Dropdowns cleaned — removed 5 decorative/Enum-backed groups (`asset_categories`, `maintenance_categories`, `asset_statuses`, `asset_maintenance_sub_statuses`, `work_order_statuses`). Tab now 3 live groups: Maintenance Priorities (new dynamic CRUD), Usage Reading Types, FA Subclass Type Codes. Fixed priority hardcoding (was in 4 spots + backend `in:` rule). New public `GET /api/list-options/{group}` endpoint for non-Admin consumers. FA-subclass drift fixed (hardcoded 18 → DB 20). See `.kilo/plans/1783001396791-admin-lists-dropdowns-cleanup.md`. | Frontend (admin tab changed), Backend (new controller + seed migration) | ⚠️ Unacknowledged |
| 2026-07-02 | **Parts Management UI (G-02) — DONE.** `PartsView.vue` (table + category filter) + `PartDetailView.vue` (details, ERP reference rail for Admin/Manager, attachments upload/delete) replace the "coming soon" stubs. New `useParts`/`usePartDetail`/`usePartSearch` composables + `partColumns` + `PartCombobox`. `__mockParts.ts` removed; WO parts-used picker now reads live `GET /parts`. Backend `PartSeeder` (55 O&G parts across 11 categories) + seeder tests. Committed `56bd463`. See `.kilo/plans/1783038000000-parts-management-frontend.md`. | Frontend (parts views live), Backend (seeder) | ⚠️ Unacknowledged |
| 2026-07-03 | **Dashboard KPIs endpoint — `GET /api/dashboard/kpis`.** Rolling 90-day window; serves Row 2 (MTBF / MTTR / Failure Rate) + Row 3 (PM Compliance / Avg MR Duration / Avg WO Duration) + "Recently Relocated Assets" widget (latest 5). Full payload to **every authenticated role** (reuses `viewDashboard` gate). Row 1 counts stay on the existing role-adaptive `GET /api/dashboard`. New `DashboardKpiController`, `ReliabilityKpiQuery`/`ProcessPerformanceKpiQuery`/`RecentlyRelocatedAssetsQuery`, `DashboardKpiResource`; `AssetLocationHistoryResource` gained an `asset` fragment. Frontend handover: `docs/atms/04-technical/DASHBOARD_KPI_HANDOFF.md`. 11 tests, full suite 476 green. | **Frontend** (build the 9-card dashboard + relocated widget), Backend | ⚠️ Unacknowledged |

---

## 🟠 Deferred — Do Not Forget

<!-- Agreed changes that were postponed. Must include a TRIGGER — when to
     bring it back. Without a trigger, it will be forgotten. -->

| ID | What | Reason deferred | Trigger to revisit |
|---|---|---|---|
| D-001 | Rename `frontend/` → `atms/` + update Docker/nginx configs | Docs restructure done; code rename deferred per plan | When backend team starts SM subsystem |
| D-002 ✅ | Update `CLAUDE.md` to match new docs structure | — | **Done 2026-07-31** — rewritten from live code as a single root file |
| D-007 ✅ | **`views/locations/AssetLocationUpdateView.vue` DELETED (2026-08-02).** User decision: no longer needed; zero references remained in the codebase. | Orphaned when `/locations2` + `LocationsView.vue` were deleted 2026-07-31; a full view is not removed on assumption | **Done 2026-08-02.** |
| D-008 ✅ | **Proper Booking model — BUILT (2026-07-31).** Dedicated `bookings` table with date range, job reference, booked-by user, status lifecycle (`active`/`cancelled`/`released`), overlap detection **with force-override warning**, auto-release on deactivation/withdrawal, full history. `is_booked` on assets is now derived. Endpoints: `GET/POST /assets/{id}/bookings`, `PUT /assets/{id}/bookings/{booking}` (edit), `POST /assets/{id}/bookings/{booking}/cancel`. Frontend: Bookings card in right rail (Reference + Status rows), click → detail Dialog (all fields), Edit button → pre-filled form Dialog, overlap → 409 with conflicts → "Book Anyway" force button. `AssetIdentityBadges` gained a slot for inline "Booked" badge in asset list. 933 tests green, Pint clean, vue-tsc clean. | Operations book up to 3 months ahead but `is_booked` was a bare boolean — no dates, no job link, no record of who committed the asset, no overlap detection, no history. | **Done 2026-07-31.** Overlap is a warning (user can force), not a hard block. |
| D-009 | Asset **status history** table | Needed only if LDC wants status as-of a past date; today `operational_status`/`maintenance_status` are overwritten in place and history exists only in `audit_logs` blobs | LDC confirms Report 1 date-range semantics (point-in-time vs created/updated-in-range) |
| D-010 | Report **export** (CSV / PDF / xlsx) | CSV mechanism exists (`CsvReportStreamer`) and is wired to **3 of 21** reports (R-1, R-2, R-22); the remaining 18 are backend-only column-map work. PDF can reuse the `PartRequestPrintView` print-route pattern; xlsx needs a new dependency | LDC confirms acceptable format. |
| D-011 ✅ | **WO Forms route by Maintenance Category — BUILT 2026-08-01.** `form_template_maintenance_category` pivot with mirrored `is_active` + partial unique index; `FormTemplateCategoryPivot` owns the mirror and the conflict guard (422 on create, 409 on reactivate/edit, naming the category and the owning form). `SnapshotFormTemplateIntoWorkOrder` + `SyncWorkOrderFormToLatest` re-pointed; the 4 `/admin/fa-subclass-type-codes` routes + controller deleted. Existing templates migrated deactivated with no categories, and cannot be reactivated until reassigned. Coverage is editable after creation. | **Not that the column is dropped — that it isn't ATMS's.** `fa_subclass_code` is written by the ERP sync so ATMS cannot govern it; `maintenance_categories` is ATMS-owned. A field ATMS doesn't control must not route behaviour ATMS is accountable for. Subclass **stays** as a read-only report dimension in 7 reports. | **Done 2026-08-01.** |
| D-012 ✅ | **PM Rules assignable to Maintenance Categories — BUILT 2026-08-01.** `pm_rule_maintenance_category` pivot + `origin`/`source_maintenance_category_id` on `asset_pm_assignments`. `ReconcilePmCategoryAssignments` expands the link into rows; `ReconcilePmCategoryAssignmentsJob` runs it off the request, overlap-keyed per scope, chunked both directions. Hooks: rule create/update/deactivate/reactivate, asset category / `is_active` / `maintenance_status` change, and the workbook import (once at the end). One audit entry per run, not N. | Materialize, not resolve: `CreateAssetPmAssignment` stamps `last_triggered_date = now()` for one interval of grace, and dynamic resolution has no moment to do that. Precedence: reconciliation may restore only what it withdrew itself — `assigned_by`/`deactivated_by` being null is what marks its own work. | **Done 2026-08-01.** |
| D-013 ✅ | **`EvaluatePmRulesJob` scaling — FIXED 2026-08-01.** `PmEvaluationBatch` builds the readings/suppressions maps the calculator always accepted; `PmEvaluationRunner` checks due-ness before opening a transaction; the job chunks ids and fans out per-chunk child jobs; `isTriggeredBy*` now take the collections they used to hardcode null for. `evaluateAll` routed through the same runner. | **The batch path was built and unused.** 400 assets ≈ 1,600 assignments ≈ 10–19k queries against `timeout = 300` — it would not finish, and D-012 is exactly what turns a handful of assignments into thousands in one click. | **Done 2026-08-01.** Query count is flat: identical at 5 and 25 assignments, pinned by `PmEvaluationScaleTest`. |
| D-014 | **Re-enable reading-based PM triggers.** Parked 2026-08-04: `PmRuleForm.vue` `TRIGGER_OPTIONS` holds only the Calendar option. Backend contract, calculator, and existing rules' reading config all intact — re-enable by restoring the `reading` / `date_or_reading` entries. | Reading-based PM is unreachable through the UI: readings can only be recorded on the WO page and no UI path verifies them (confirm endpoint never called), so the reading dimension could never fire. Parking avoids misleading admins while date-only PM works. | **LDC Job Management system feeds per-job asset usage (operating hours, km, depth) into ATMS.** ✅ **Verification is no longer a blocker** — closing a work order now confirms its readings (2026-08-04, see 🟡 above), so confirmed readings exist for anything recorded on a WO. Two things still gate re-enablement: **D-018** (first reading seeded as a delta — must be fixed before real odometer values arrive) and reading entry outside work orders, which the Job Management feed itself supplies. Note an externally-uploaded reading has **no work order to close**, so the feed needs its own confirm path. Plan: `docs/plans/2026-08-04-pm-reading-trigger-parking.md`. |
| D-015 | **Requester meter-reading entry on the MR create form.** Backend is already built: `MaintenanceRequestController` validates an optional `meter_reading` object and `CreateCorrectiveMaintenanceRequest` records it; `AssetMeterReadingPolicy::create()` allows Requester precisely for this. `useMaintenanceRequests.ts` `doCreate()` never sends the key, so the capability is unreachable. Also needs: reading types via `useListOptions().loadReadingTypes()` (role-open endpoint, **not** the Admin-gated one), the draft reset added to `closeCreate()`, and `work_order_id` backfilled onto the MR's readings at approval (chosen over widening the confirm rule to `work_order_id OR maintenance_request_id`, because `maintenance_request_id` has **no FK constraint** and is too shaky to gate PM). | With PM reading-triggers parked (D-014), requester-entered readings feed only R-20 and the Asset Usage report. Also blocked on an unresolved design question: the WO form takes a **delta**, an MR form would take an **absolute** — two mental models for one field, aimed at the least-trained role. | **D-014's trigger fires** (Job Management feed) **or** LDC asks for requester readings. Settle delta-vs-absolute first, and fix D-018 before either. |
| D-016 | **WO cancel dialog — opt-in "verify these readings".** Cancel dialog (`WorkOrderDetailView.vue`) gains an unchecked-by-default checkbox when the WO holds unverified readings; `verify_readings` boolean on `POST /work-orders/{id}/cancel` runs the same `ConfirmWorkOrderReadings`. Agreed rule: close confirms **automatically** (job done and signed off), cancel **asks** (job wasn't done, but the meter value may still be accurate). New markup, not a reuse — the `readingBelowLast`/`lowerReadingAcknowledged` acknowledge pattern was removed by the Phase D delta change. | Only bites when a cancelled WO holds readings — rare. Close covers the normal path. | A cancelled work order is found holding readings that someone wants counted. |
| D-017 | **User-manual rewrite for the new verification rules.** `frontend/src/content/user-manual.md` (~L2160-2195) still describes confirmation as a manual review step — *"A Technician or Manager must review and confirm"* — and documents requester MR readings, which remain unreachable (D-015). | Close-confirms is live but the surrounding rules (D-015, D-016) are not, so a rewrite now would document a half-state. | D-015 or D-016 ships — rewrite once, covering whatever the final rule set is. |
| D-018 | **🐛 First meter reading is seeded as a delta.** `useWorkOrderDetail.ts` `draftTotal` computes `base ? base.value + delta : delta`, so with no prior reading for that asset+type the entered delta **becomes the absolute total**. An operator typing "8" (hours since last service) records a lifetime meter value of 8 when the dial reads 1240. The UI already knows there is no base (`lastReadingForDraft` is null) — it just answers "treat the delta as the total" instead of changing the question to "what does the meter read now?". | Fails **quietly**: the reading dimension never reaches its threshold and the calendar dimension carries on, so nothing surfaces. Currently masked entirely by D-014 parking. | **Before D-014's trigger fires.** 10 readings exist across 5 of 400 assets, so essentially every reading in the near future is a first reading — a wrong seed poisons the accelerator the Job Management feed is meant to provide. |
| D-004 ✅ | Virtual Store resolved — one workshop, per-part approval, auto-flag, overnight hold with next-day enforcement | 2026-06-24 | Done — spec in `docs/sm/01-product/VIRTUAL_STORE.md` |
| D-019 | Add `docs/ROADMAP.md`/`docs/REQUIREMENTS.md` entries for the agreed status-vocabulary design; refresh `docs/README.md`'s verification date | **Reopened 2026-08-16** — the trigger fired (a design is agreed). ROADMAP "Next step" synced; the four LDC-blocking external-dependency rows were **removed 2026-08-16** because LDC answered them (Q1/Q2 `at_the_field` rules, Q4 approval location, Q5 PM marking, Q6 quantity ownership) and replaced by a single Q7 row. The remaining registry entries and the README verification-date refresh land with the vocabulary release. | A status-vocabulary design is agreed (true since 2026-08-16). |
| D-020 | **ERP sync will overwrite locally-decremented part quantities.** Q6 made ERP the quantity authority: `SyncParts` (`app/Actions/Erp/SyncParts.php:62`) writes `available_quantity` wholesale from the ERP payload, and `SyncErpPartsJob` is scheduled weekly (Mondays 03:00, `routes/console.php:14`). The WO decrement/restore shipped in the vocabulary work is therefore **interim honesty between refreshes**, not a ledger — the same is true of the RQ3 CSV upload. | Accepted deliberately, not overlooked: LDC chose ERP authority with full knowledge that the local decrement is transient. Recording it so a future session does not "fix" the overwrite or mistake the decrement for stock accounting. | **`LDC_ERP_PARTS_API` is configured** (today it is unset, so the sync cannot reach ERP and the decrement is the only writer). On that day, verify the consumption report still reconciles and decide whether a true consumption ledger is wanted. |
| D-023 | **Moving the default value of a master-data vocabulary.** `master_data_items.is_default` is writable only by migration — no endpoint accepts it, and `PATCH /admin/master-data/items/{item}` deliberately does not. An Admin can rename the default and can retire any other row, but cannot promote a different one. Building it means an action that clears the old default and sets the new one in one transaction (the partial unique index `UNIQUE (group_key) WHERE is_default` rejects two), plus policy, tests and a frontend control. | Deliberate, not an oversight: exactly one default exists per group and both were chosen with LDC. Shipping a swap control now would be scope with no user waiting for it. The deactivation guard's message was reworded so it no longer promises a control that does not exist. | **LDC wants a reset target other than Normal** — i.e. the day someone asks to change what a work-order close resets an asset's condition to. |
| D-024 | **Weekly ERP sync for assets (Phase 3) — two things to get right when it lands.** Confirmed 2026-08-17: ERP becomes authoritative for assets as well as parts, on a weekly sync, roughly six months out subject to LDC budget. **(a) Precedence.** Once ERP owns `is_active`, a manager's manual deactivation in ATMS is overwritten on the next run — and so is a manual *re*activation of something ERP calls disposed. This is the same shape as 🟠 D-020 (ERP overwriting locally-decremented part quantities) and as the PM-reconciliation rule that a null `assigned_by` marks the system's own row while a filled one marks a person's. Decide who wins before the sync writes anything. **(b) Model events must fire.** `Asset::booted` releases active bookings on deactivation, and `SyncParts` gets this right today by using Eloquent (`firstOrNew` → `fill` → `save`) rather than raw upserts. An asset sync that switches to raw SQL for throughput would deactivate assets while leaving live bookings behind — the same trap the 4b value migration had to handle explicitly. | Recording it now because both are invisible until they bite: the first shows up as assets mysteriously coming back to life, the second as bookings nobody can clear. Neither is discoverable from the sync code alone. | **Phase 3 asset-sync design starts** (~6 months from 2026-08-17, budget permitting). **(c) The mapping itself is settled.** LDC confirmed 2026-08-17 that ERP-inactive is *permanent* — disposed, LIH, DBR or sold, written off financially — and never means temporarily away, so `is_active = ! inactive` is safe and needs no interpretation layer. **(e) Assets are never deleted** (LDC policy): the sync deactivates, it must not prune. |

> **Note:** Asset tag QR code generation (was D-003), SM architecture/parts
> catalogue (was D-005), and ERP parts write-back (was D-006) have been
> assigned concrete phases — see the Phase 2 / Phase 3 tables below.

---

## 🟢 Done

<!-- Move items here once all teams have acknowledged. Keep last 10. -->

| Date | What | Completed by |
|---|---|---|
| 2026-06-28 | VPS frontend bug tracker + notification integration spec + docs README updated | AI-assisted |
| 2026-06-24 | Documentation restructure (3 subsystems, 5 roles, 19 files updated) | AI-assisted |
| 2026-06-24 | ERP connection tested: token acquired, 429 assets fetched from BC | AI-assisted |
| 2026-06-24 | Mock ERP env vars removed from compose.yaml, .env, backend/.env | AI-assisted |
| 2026-06-24 | `RISKS_AND_ASSUMPTIONS.md` updated with real ERP details | AI-assisted |
| 2026-06-24 | LDC meeting prep document (parts write-back) + PDF | AI-assisted |
| 2026-06-25 | Sidebar navigation redesign (flat + tabs, 7 items, 5 docs rewritten) + Secure Remote API Access spec (new doc, 3 docs updated) | AI-assisted |

---

## 🔵 External Blockers

<!-- Things we cannot advance until someone outside the team provides
     something. Full details in docs/05-delivery/TDL.md. -->

| # | What | Waiting on |
|---|---|---|
| 1 | Parts API page name (BC custom API page) | ERP team / LDC |
| 2 | Parts field mapping (response schema) | ERP team / LDC |
| 3 | `componentOfMainAsset` sample with non-null parent | ERP team / LDC |
| 5 | **Dashboard/Reports clarifications sent to LDC (2026-07-31), awaiting reply:** (a) which axis "asset status" means — booking, operational, or enrolment — since ATMS models three independently; (b) do bookings have start/end dates and a job reference, and should overlaps be blocked; (c) does Report 1's date range mean point-in-time status (needs new history table) or current status created/updated in range; (d) Report 1 lists "Assigned To" but assets have no custodian field — does it mean the technician on the open WO; (e) intended users + full requirements for Reports 2 and 3; (f) is CSV acceptable in place of xlsx | LDC |
| 4 | ~~Power Automate webhook URL~~ → **Graph email remaining: Application Access Policy** (restrict app to `notification@ldc.com.ly`) + **official LDC frontend subdomain** for email links. Both now gate MR/WO workflow email as well as account email — see R-007. | LDC IT (Exchange admin) / LDC (subdomain) |

---

## Process

1. **Start of build session:** Read this file. Pick up anything in 🔴 In Progress
   or pull from 🟠 Deferred if its trigger fired.
2. **During build:** If a requirement surfaces that needs the other team, add it
   to 🟡 immediately — do not assume you will remember.
3. **When postponing:** Add to 🟠 Deferred with a clear trigger. "Later" is not
   a trigger. "After asset_tag migration is merged" is a trigger.
4. **End of session:** Move completed items to 🟢 Done. Move 🟡 items that were
   acknowledged. Review 🟠 for any triggers that fired.

## Phase 1.5 — Inspection management (CANCELLED 2026-08-16)

> ⛔ **Cancelled 2026-08-16:** "Inspection" for LDC means PM, not
> third-party certificates. The P2-011 certificate register is dropped —
> inspection becomes a WO form + attachment with the executed PM marked
> (plan doc §7, RQ1/RQ2). The 2026-08-14 Phase 1.5 promotion and its
> estimate are void.

## Phase 2 (deferred)

| ID | What |
|---|---|
| P2-001 | Asset Assembly: install/remove/swap Actions, assembly_history table (`parent_asset_id` column already exists in schema); `installed`/`ready` become **derived from `parent_asset_id`** (sub-status deprecated 2026-08-16) |
| P2-002 | Component PM cross-check: 🟢🟡🔴 indicators + "Create MR for Component" |
| P2-005 | AM movement workflow: Requester → Logistics approve → confirm arrival |
| P2-006 | ERP parts write-back from SM GR to BC ERP (ERP team must confirm consumption/decrement API contract) |
| P2-008 | Asset tag QR code generation on asset detail page (format `L-BBB-CCC-XXXX` already decided; visual rendering is the remaining work) |
| P2-009 | WO **waiting-for-parts**: explicit flag on `work_orders` + close guard ("cannot close while waiting for parts") + **time-waiting KPI** per asset. The KPI requires **timestamped transitions** (`waiting_for_parts_at` / cleared-at), not a bare boolean. Phase 1 note: out-of-stock parts cannot be requested today (`RecordWorkOrderPart` rejects them), so "waiting" cannot be derived from part lines — it needs the flag. |
| P2-010 | **Single open WO per asset.** Enforce at MR approval: reject (409, message naming the conflicting WO) when the asset already has an `open`/`in_progress`/`completed` WO; asset-row lock so two approvals cannot race. Today the system has zero enforcement (live data: no violations yet). Deferred with P2-001 Asset Assembly by user decision 2026-08-01; a 2026-08-07 proposal to un-defer it for a status-vocabulary design was never accepted, so it remains deferred. |

## Phase 3 — SM Subsystem (deferred)

> SM decoupled into its own phase (2026-07-02). The Store Management subsystem
> is the largest, most uncertain scope item — pending VJ's answer on whether BC
> Store Order is live (determines local `parts` table vs direct BC query).

| ID | What |
|---|---|
| P3-001 | SM architecture + parts catalogue design — full local build vs. BC Store Order integration (pending VJ reply about BC Store Order). `work_order_parts` already exists in Phase 1 regardless. |
| P3-002 | SM Order workflow: Order → Approval → Dispatch → Goods Receipt |
| P3-003 | SM inventory management: stock movement, balances, Virtual Store |
| P3-004 | Manual Asset Creation + lifecycle-field persistence (G-01 Add Asset button + G-04 `CreateAsset` dropped fields) — **deferred to Phase 3 or cancelled** pending data-integrity decision. See gap analysis §4.1/§5.1. |
