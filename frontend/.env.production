# Production build for the single-host, same-origin deployment.
# SPA + API: https://assets.ldc.com.ly  (API under /api)
#
# Empty VITE_API_ORIGIN means relative /api and /sanctum/csrf-cookie paths —
# same-origin. See src/lib/api.ts.
VITE_API_ORIGIN=
