services:
  app:
    restart: unless-stopped

  web:
    restart: unless-stopped

  postgres:
    restart: unless-stopped

  queue:
    restart: unless-stopped

  scheduler:
    restart: unless-stopped
